version: 0.2

phases:
  install:
    runtime-versions:
      goland: 1.22
    commands:
      - echo installing dependencies..
      - go mod tidy
  build:
    commands:
      - echo Build started on `date`
      - make usersvc
  post_build:
    commands:
      - echo Build completed on `date`